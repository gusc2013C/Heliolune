interface:
  display_name: "Heliolune"
  short_description: "Track Luna work without Sol polling"
  default_prompt: "Use $luna-pool-orchestrator for one bounded task: start it with visible Leader status, await it once without polling, and return a compact result to Sol."
